// operador ternario

const pontuacaoUsuario = 1000; 
const nivelUsuario = pontuacaoUsuario >= 900? true : false;

console.log(nivelUsuario, corPadrao);

// antes da ? devo por uma condição, depois da ? ponho oq espero de retorno condicional 

//atribuindo padrão para uma variavel com operador ternario 
const corDoUsuario = null
const corPadrao = corDoUsuario || "preto"
